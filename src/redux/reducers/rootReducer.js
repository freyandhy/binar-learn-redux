// Initial State
const globalState = {
  keyword: null,
  movies: null,
  isLoading: false,
  isError: false,
};

// Reducer
const rootReducer = (state = globalState, action) => {
  return state;
};

export default rootReducer;
